{
  "name": "@codex-remote/mac-bridge",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "npm run build --workspace @codex-remote/protocol",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "pretest": "npm run build --workspace @codex-remote/protocol",
    "test": "node --test --import tsx test/**/*.test.ts"
  },
  "dependencies": {
    "@codex-remote/protocol": "0.1.0",
    "dotenv": "^16.4.7",
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/ws": "^8.5.14",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
